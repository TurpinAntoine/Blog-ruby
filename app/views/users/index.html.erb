<h1>Index Users</h1>

<ul>

<% @users.each do |user| %>

   <li><%= user.username %></li>
    <%= link_to t('views.index.users.link'), user_path(user.id) %>
    <br>
    <hr>
    <br>
<% end %>

</ul>
